<%= render 'books/list1', user:@user %>
<%= render 'books/list2', book:@book %>

<% @books.each do |book| %>
<div>
  <%= link_to book_path(book.id) do %>
  <%= attachment_image_tag book, :image %>
  <% end %>

  <p>title：<%= book.title %></p>
  <p>opinion：<%= book.body %></p>

</div>
<% end %>